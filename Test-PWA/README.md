# Test PWA

A minimal software toy that follows `CONTRACTS.md` and `Architecture.md` and is intended for smoke tests.

How to run:
- Start local server: `npm run serve`
- Open `http://localhost:8000/Test-PWA/index.html`

Smoke tests can target `tests/smoke/test-pwa-*.spec.js` once added.
